// const chai = require('chai');
// const expect = chai.expect;
// const AuthController = require('./AuthController');

// describe('AuthController', () => {
//     it('should return a token on successful login', () => {
//         const result = AuthController.login('username', 'password');
//         expect(result).to.have.property('token');
//     });

//     it('should throw an error on failed login', () => {
//         expect(() => AuthController.login('wrongUser', 'wrongPass')).to.throw('Invalid credentials');
//     });
// });